<Window x:Class="CalcGains.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CalcGains"
        mc:Ignorable="d"
        Title="MainWindow"
        Loaded="Window_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="100"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="500"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button
            Grid.Column="0"
            Grid.Row="1"
            Content="Dodaj produkt"
            Name="AddProductButton"
            Click="AddProductButton_Click" />
        <Button
            Grid.Column="1"
            Grid.Row="1"
            Content="Dodaj posiłek"
            Name="AddMealButton"
            Click="AddMealButton_Click"/>
        <Button
            Grid.Column="2"
            Grid.Row="1"
            Content="Oblicz zapotrzebowanie"
            Name="CalculateDietButton"
            Click="CalculateDietButton_Click"/>
    </Grid>
</Window>
